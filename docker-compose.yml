version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"

  proxy:
    build:
      context: ./nginx
    ports:
      - "8080:8080"
    depends_on:
      - app

volumes:
  static_data: